Icon sources
============

- Brand SVGs are downloaded from Simple Icons through jsDelivr.
- Interface SVGs are downloaded from Heroicons solid through unpkg.
- Local copies are normalized to the site's monochrome palette and bundled from `assets/icons/`.
- Technology stack SVGs live under `assets/icons/tech-stacks/`.
